# 2. You have a code. Write down a complexity near each line
# def array_arithmetic(array):
#   value = 0 #
#   for e in range(len(array)):              # O(len(array))
#     for j in range(10):                            # O(10)
#       for k in range(len(array)//2):            # O(len((array)//2)
#         value += array[e] + array[j] + array[k] #
#   return value
# O(n*n)